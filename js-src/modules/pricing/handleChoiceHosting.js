/**
 *
 * @returns {Promise<any>}
 */
export default function() {

    return new Promise((resolve, reject) => {
      // TODO: hide this fieldset en show it only when previous step has been done

      console.log('now the hosting can be done');

      resolve();
    });

};
